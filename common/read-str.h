// A simple module that provides the read_str function
// from the CS136 course notes

// read_str() reads in a new string from I/O or returns NULL if EOF or
//   only whitespace is encountered
// effects: may allocate memory (caller must free)
char *read_str(void);
